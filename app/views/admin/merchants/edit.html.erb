<h2> Edit <%=@merchant.name %> </h2>

<%= form_with url: "/admin/merchants/#{@merchant.id}?change=name", method: :patch, data:{turbo: false} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, :value => @merchant.name %>
  
  <%= f.submit "Submit" %>
  <% end %>